# PostgreSQL local dev defaults
# Copy to your shell environment (or use a local .env file outside git)

AGENT_STUDIO_PG_PORT=5432
AGENT_STUDIO_PG_DB=agent_studio
AGENT_STUDIO_PG_USER=agent_studio
AGENT_STUDIO_PG_PASSWORD=agent_studio
